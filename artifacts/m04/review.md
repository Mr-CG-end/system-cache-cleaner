# M04 修复复核记录

- 复核日期：2026-07-23
- 复核 Agent：Codex
- 模块：M04 扫描结果与项目选择
- 当前结论：自动验收通过，界面手工项待复核

## 已完成修复

1. 每次新扫描清空旧结果后立即把选择数量和预计释放空间重置为 0。
2. 重新扫描发生异常时保持空结果和 0 B 汇总，不再残留上一次扫描快照的汇总。
3. 增加“首次扫描成功、第二次扫描失败”的回归测试，验证旧结果、旧汇总和清理按钮状态均被正确清除。

## 自动验证

- M03/M04 定向测试：
  - 结果：通过，18/18 测试通过，0 失败，0 跳过。
- 完整回归测试：
  - 结果：通过，30/30 测试通过，0 失败，0 跳过。

## 验收覆盖

- AC-M04-02：默认选择来自类别定义，用户临时文件和 Edge 默认选中，Windows 临时文件默认不选中。
- AC-M04-03、AC-M04-05、AC-M04-06：选择变化立即更新汇总；全部取消或选中项合计 0 B 时清理命令禁用。
- AC-M04-04：0 B、KB、MB、GB 和负数边界格式化测试通过。
- AC-M04-07：取消结果标记为不完整，清理命令保持禁用。
- AC-M04-01、AC-M04-03、AC-M04-05～08 的桌面交互表现仍需在可用的 Windows 桌面自动化或人工环境中最终复核。

## 环境限制

Windows 桌面自动化原生控制管道不可用，无法生成可靠的界面操作与截图证据。因此本记录没有将界面手工项标记为已通过。

